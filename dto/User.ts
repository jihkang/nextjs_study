export type UserSignInDto = {
    email: string;
    password: string;
    // id        String      @id
    // email     String      @unique
    // name      String?
    // password  String
    // createdAt DateTime    @default(now())
    // updatedAt DateTime    @updatedAt
    // posts     Post[]
    // profile   Profile?
}